# Future-Of-Data-2024-Hackathon
hello world

pip install Flask, flask-cors | flask-cors since the React app runs on localhost:3000 and the Flask app runs on localhost:5000. CORS is Cross-Origin Resource Sharing